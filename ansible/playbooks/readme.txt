The playbook directory is where your playbooks live.
group_vars contain yaml files describing group variables applied to a playbook.
host_vars contain yaml files describing individual hosts variables applied to a playbook.